#include "sum.h"

// sum library implementations
int sum(int a, int b) {
    // this is just a "stub" now--it's a small implementation 
    // of this function that compiles, but is incorrect
    return a + b;
}